# 问题清单

- 版本：v1.0.0
- 生成时间：2026-03-07 03:07:35 CST
- 变更范围：`git diff --name-status` 当前所有变更 + 未跟踪文件（本次无未跟踪文件）
- 审查级别：D3 / R4 / C1 => **L4**

## 子-agent-A（clean-code-reviewer）输出

### 审查范围

| 分类 | 文件数 | 文件 |
|---|---:|---|
| Modified | 20 | `src/common/constants/domain/reference-data.constants.ts`; `src/core/01-entry/receiver/services/receiver.service.ts`; `src/core/01-entry/stream-receiver/services/stream-receiver.service.ts`; `src/providersv2/provider-id.constants.ts`; `src/providersv2/providers/longport/capabilities/get-stock-quote.ts`; `src/providersv2/providers/longport/capabilities/stream-stock-quote.ts`; `src/providersv2/providers/longport/types.ts`; `test/unit/core/01-entry/stream-receiver/services/stream-receiver.service.spec.ts`; `test/unit/providersv2/provider-assembly.sync.spec.ts`; `test/unit/providersv2/provider-id.constants.spec.ts`; `test/unit/providersv2/provider-registry.service.spec.ts`；以及文档/说明类变更文件 |
| Deleted | 10 | `src/providersv2/providers/longport-sg/**` 全部实现文件；`test/unit/providersv2/longport-sg/longport-stream-context.service.on-quote-update.spec.ts` |
| Untracked | 0 | 无 |

### Critical

- 无。

### Important

- 无。

### Minor

1. `src/providersv2/provider-registry.service.ts:31`
   - 风险：注释仍写“向后兼容历史命名”，但当前 `PROVIDER_NAME_ALIASES` 已不含 `longport-sg/longportsg`（且测试明确禁止），注释与真实行为不一致，易误导维护者。
   - 建议：更新注释为“仅解析 manifest 显式声明别名”，避免暗示隐式兼容。

2. `src/core/01-entry/stream-receiver/services/stream-receiver.service.ts:1778`
   - 风险：`STREAM_MARKET_PROVIDER_PRIORITIES` 目前所有市场均固定为 `LONGPORT`，而同文件存在针对 `JVQUANT` 的评分/可用性分支；策略层存在“看似多提供商、实际单提供商”的认知落差。
   - 建议：要么将策略相关注释标注为“当前单提供商模式”，要么在策略入口显式断言当前仅单候选，减少误判。

### Strengths

1. LongPort-SG 清理较完整：代码引用层面未发现 `LONGPORT_SG|longport-sg|longportsg` 残留。
2. Provider 清单、优先级与装配一致性由单测覆盖（`provider-assembly.sync.spec.ts`）。
3. SG 路由到 canonical provider 的关键路径已有测试覆盖（registry + stream receiver）。
4. 构建与关键单测均通过，未发现编译断裂。

### Verdict

- 结论：**可进入下一阶段（问题真实性复核与修复计划）**。
- 合并门槛：当前无 Critical/Important；建议先处理 2 条 Minor 以提升可维护性一致性。

## 子-agent-B（code-reviewer 工作流）输出

### 预设步骤清单

| Step | 目标 | 命令 |
|---:|---|---|
| 1 | 识别审查输入边界（diff/untracked） | `git diff --name-status` + `git ls-files --others --exclude-standard` |
| 2 | 运行 Pr Analyzer 等价流程（变更影响面梳理） | `git diff -- src/**/*.ts test/**/*.ts` |
| 3 | 运行 Code Quality Checker 等价流程（编译/关键测试） | `bun run build` + 定向 `jest` |
| 4 | 人工规则复核（安全、边界、耦合、复用） | 对 modified/deleted 文件逐项复核 |
| 5 | 生成报告（等级化问题清单） | 结构化输出 Critical/Important/Minor/Strengths/Verdict |

### 实时进度同步

- Step 1/5 - 完成 - 已确认 20 modified、10 deleted、0 untracked。
- Step 2/5 - 完成 - 已提取 TS 与测试差异，核心影响集中在 provider manifest、stream provider 选择、SG 市场映射。
- Step 3/5 - 完成 - `bun run build` 通过；定向单测 4 组通过（26+5 tests）。
- Step 4/5 - 完成 - 未发现安全漏洞或边界缺失导致的高风险回归；发现 2 条可维护性 Minor。
- Step 5/5 - 完成 - 输出等级化结论并提交 C 汇总。

### B 结论

- Critical：0
- Important：0
- Minor：2（与 A 收敛）
- 建议：进入真实性复核与原子修复规划。

## 子-agent-C 汇总（A+B 去重结果）

| 分类 | 数量 | 摘要 |
|---|---:|---|
| Critical | 0 | 无 |
| Important | 0 | 无 |
| Minor | 2 | 注释语义漂移 1 条；策略语义一致性 1 条 |
| Strengths | 4 | 清理完整、装配一致、SG 路径有测、构建与关键测试通过 |
| Verdict | 1 | 可进入修复规划，优先清理 Minor 提升长期维护性 |

### 详细描述（去重后）

#### Minor-1：Provider alias 注释语义漂移
- 文件：`src/providersv2/provider-registry.service.ts:31`
- 现象：注释描述“向后兼容历史命名”，与当前移除 legacy alias 的实现不一致。
- 影响：维护者可能误判系统仍兼容 `longportsg` 旧入口。
- 建议修复：更新注释为“仅处理 manifest 声明别名”，避免误导。

#### Minor-2：策略表达与候选集现状不一致
- 文件：`src/core/01-entry/stream-receiver/services/stream-receiver.service.ts:1778`
- 现象：市场优先级映射已单一化（仅 longport），但策略函数仍保留多 provider 打分叙事。
- 影响：后续维护时对真实运行路径产生认知偏差。
- 建议修复：补充注释或显式断言“当前单候选模式”，保持代码语义一致。
