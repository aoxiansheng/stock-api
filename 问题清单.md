# 问题清单

- 版本号：v1.1.0
- 时间戳：2026-03-06 23:41:10 (Asia/Shanghai)

## 五级分类总表

| 分类 | 数量 | 问题来源 | 当前状态 | 结论摘要 |
| --- | ---: | --- | --- | --- |
| Critical | 0 | 存量 0 / 本轮新增 0 | 无未关闭项 | 无本轮新增 Critical 回归。 |
| Important | 1 | 存量 1 / 本轮新增 0 | 待治理 | 存量 lint 技术债依然存在（1419 errors）。 |
| Minor | 1 | 存量 1 / 本轮新增 0 | 可优化 | 测试命名与职责边界可读性仍有提升空间。 |
| Strengths | 3 | 本轮确认 3 | 正向 | 无本轮新增 Critical/Important 回归；`no-unused-imports` 规则不存在问题已修复；verification 4 条反向断言修复有效。 |
| Verdict | 1 | 汇总结论 | 条件通过 | 回归修复方向正确，但需持续清理存量 lint 技术债。 |

## 详细问题说明

### Critical

- 当前无未关闭 Critical 问题。
- 结论：本轮新增 Critical 回归为 0。

### 1) Important：存量 lint 技术债仍未清零（1419 errors）

- 问题属性：**存量问题**（非本轮新增）。
- 背景：`no-unused-imports` 规则不存在导致的配置阻断已消除，lint 已回到“真实代码问题”暴露阶段。
- 证据：`bun run lint` 仍报告 1419 errors（较上一轮 1420 下降 1）。
- 影响：
  - 质量闸门尚未恢复到可稳定放行状态。
  - 后续回归与重构评审会被大量存量告警噪声影响效率。
- 建议：
  - 采用分批治理策略（按模块/规则分治），优先处理核心链路与高频规则。
  - 在 CI 中结合增量门禁（仅阻断新增错误）与全量债务燃尽计划并行推进。

### 2) Minor：测试命名与职责边界可优化

- 问题属性：**存量问题**（非本轮新增）。
- 背景：部分测试命名未稳定体现“场景 + 行为 + 预期”，职责分组一致性一般。
- 影响：
  - 用例可读性和可维护性下降。
  - 后续扩展时更容易出现重复测试或语义重叠。
- 建议：
  - 统一测试命名模板并按职责拆分目录（行为验证/兼容性验证/回归验证）。
  - 对关键验证文件补充意图注释，降低维护歧义。

## Strengths（本轮有效修复与正向结论）

### S1) `no-unused-imports` 规则不存在问题已修复

- 条目属性：本轮新增结论（正向）。
- 结果：Lint 不再因该规则缺失而配置级失败。

### S2) verification 4 条反向断言修复有效

- 条目属性：本轮新增结论（正向）。
- 结果：`test/unit/_verification/critical-group.verification.spec.ts` 中“期望旧缺陷存在”的反向断言问题已修复，验证行为与当前预期对齐。

### S3) 无本轮新增 Critical/Important 回归

- 条目属性：本轮新增结论（正向）。
- 结果：第二轮 A/B 汇总未发现新的 Critical/Important 级别回归。

## Verdict

- 结论：本轮修复有效，且**无本轮新增 Critical/Important 回归**。
- 仍需关注：**存量 lint 技术债（1419 errors）依然存在**，建议纳入持续治理计划并分批燃尽。

## 审查范围与执行命令记录

- 审查范围（结论汇总覆盖）：
  - 质量闸门：Lint 配置与执行可用性。
  - 单元验证：`test/unit/_verification/critical-group.verification.spec.ts`。
  - 重构关键链路：`provider-registry` / `receiver` / `query` / `stream-data-fetcher`。
- 执行命令记录（用于得出本次结论）：
  - `bun run lint`
  - `bun run test:unit -- test/unit/_verification/critical-group.verification.spec.ts`

## 去重说明

- 已将“规则不存在导致 lint 配置级失败”从未解决问题中移除，并转入 Strengths（已修复项）。
- 已将“4 条反向断言修复”统一归并为单一正向结论条目（S2），避免重复描述。
- 保持“存量 lint 技术债”作为唯一 Important 主问题，避免同类告警重复计数。
