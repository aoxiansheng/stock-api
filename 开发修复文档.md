# 开发修复文档

- 文档版本: v4.0.0
- 写入时间: 2026-03-09 02:43:00 CST
- 来源: 第4轮 agent-A 审查 + agent-B 验真

## 优先级

| 优先级 | 问题 | 级别 |
|---|---|---|
| P1 | 主链路与分时域事件广播耦合（阻塞） | Important |
| P1 | WS/HTTP cursor 密钥策略不一致 | Important |
| P1 | 显式 market 与 symbol 后缀未做一致性校验 | Important |
| P2 | cursor DTO optional 与运行时必填不一致 | Minor |
| P2 | 高频 DateTimeFormat 分配开销 | Minor |

## 原子计划

1. `stream-client-state-manager.service.ts`
- 主流程中异步调度分时域事件广播，主 data 广播不等待分时广播完成。
- 统一 cursor secret 解析策略为仅 `CHART_INTRADAY_CURSOR_SECRET`，删除 fallback 常量。
- 保持异常隔离日志。

2. `chart-intraday.service.ts`
- `resolveContext` 增加 market/symbol 一致性校验。

3. `intraday-delta-request.dto.ts`
- `cursor` 改为必填契约（Swagger + Validator）。

4. `market-time.util.ts`
- 缓存 `Intl.DateTimeFormat` 实例（按时区键）。

5. 测试
- `chart-intraday.service.spec.ts`：补 market/symbol 一致性用例。
- `stream-data-fetcher.service.close.spec.ts`：补主流程不阻塞与密钥一致性用例。
- 受影响回归 + compile 全通过。
