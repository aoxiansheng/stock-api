# 开发修复文档

- 文档版本：v1.3.0
- 生成时间：2026-03-07 15:24:14 +0800
- 输入文档：[问题清单.md](/Users/honor/Documents/code/newstockapi/stock-api/问题清单.md)
- 执行约束：绿地项目、禁止兼容回退逻辑、最小成本、第一原理、禁止过度工程化、不得新增文件

## 第二步：D/E/F 并行子任务结果（主-agent 复核后收敛）

### 子-agent-D：问题真实性校验
- 结论：无确认问题（Critical/Important/Minor 均为 0）。

### 子-agent-E：影响面分析
- 因无确认问题，不存在负向影响面扩散。
- 验收关注域：
1. `src/providersv2/provider-registry.service.ts`
2. `src/providersv2/provider-priority-policy.service.ts`
3. `src/providersv2/provider-priority-policy.cache.ts`
4. `src/core/01-entry/stream-receiver/services/stream-receiver.service.ts`
5. 对应 `test/unit/providersv2/*.spec.ts`
6. 对应 `test/unit/core/01-entry/stream-receiver/services/stream-receiver.service.spec.ts`

### 子-agent-F：原子修复计划
- 无修复任务。
- 最小验收计划：
1. providers 域定向覆盖率测试。
2. stream-receiver 域定向覆盖率测试。
3. providers + stream 回归抽测。
4. TypeScript 构建验证。

## 主-agent 合并去重与优先级排序

| 优先级 | 数量 | 说明 |
|---|---:|---|
| P0 | 0 | 无 |
| P1 | 0 | 无 |
| P2 | 0 | 无 |
| P3 | 0 | 无 |

## 第三步：按文件域拆分并行验收（主-agent 实测）

### 任务拆分
1. providersv2 域（无代码修改）：策略与注册表链路验收。
2. stream-receiver 域（无代码修改）：选源一致性链路验收。
3. 构建任务（无代码修改）：全工程可编译性验收。

### 验收结果
1. providers 覆盖率验收：
- 命令：`npx jest --config test/jest.unit.config.js --runInBand --coverage --coverageReporters=text-summary --collectCoverageFrom=src/providersv2/provider-registry.service.ts --collectCoverageFrom=src/providersv2/provider-priority-policy.service.ts --collectCoverageFrom=src/providersv2/provider-priority-policy.cache.ts test/unit/providersv2/provider-priority-policy.service.spec.ts test/unit/providersv2/provider-registry.service.spec.ts`
- 结果：`2/2 suites`, `18/18 tests` 通过。
- 覆盖率：Statements `92.51%`，Branches `73.63%`，Functions `94.59%`，Lines `93.85%`。

2. stream-receiver 覆盖率验收：
- 命令：`npx jest --config test/jest.unit.config.js --runInBand --coverage --coverageReporters=text-summary --collectCoverageFrom=src/core/01-entry/stream-receiver/services/stream-receiver.service.ts test/unit/core/01-entry/stream-receiver/services/stream-receiver.service.spec.ts`
- 结果：`1/1 suite`, `8/8 tests` 通过。
- 覆盖率：Statements `18.50%`，Branches `16.89%`，Functions `13.55%`，Lines `19.07%`。

3. 回归抽测：
- 命令：`npx jest --config test/jest.unit.config.js --runInBand test/unit/providersv2/provider-priority-policy.service.spec.ts test/unit/providersv2/provider-registry.service.spec.ts test/unit/providersv2/provider-assembly.sync.spec.ts test/unit/providersv2/provider-id.constants.spec.ts test/unit/core/01-entry/stream-receiver/services/stream-receiver.service.spec.ts`
- 结果：`5/5 suites`, `39/39 tests` 通过。

4. 构建验收：
- 命令：`bun run build`
- 结果：通过（`tsc -p tsconfig.build.json` 成功）。

## 循环终止判定

- 条件1：连续两轮扫描无 Critical 与 Important -> 满足。
- 条件2：所有验收测试通过 -> 满足。

## 最终结论

- 当前轮次无待修复问题，不进入代码改动循环。
- 依据你定义的终止条件，本轮流程结束。
