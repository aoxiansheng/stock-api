# 开发修复文档

- 版本号：`v2.1.0`
- 时间戳：`2026-03-12 21:40:00 CST`
- 写入时间：2026-03-12
- 状态：本轮验收完成

## 1. 真实性核验结论

| 问题ID | 真实性 | 结论 |
|---|---|---|
| M-001 | ✅ 真实 | 已修复 |
| M-002 | ✅ 真实 | 已修复 |

## 2. 影响面与修复执行

## FIX-M002（已完成，优先）
- 背景：文档迁移中间态存在丢失风险。
- 修复目标：收敛为单一路径，避免“删除 + 未追踪新增”。
- 涉及文件：
  - `docs/infoway-get-stock-quote-empty-array-fix-plan.md`
  - `docs/todo/infoway-get-stock-quote-empty-array-fix-plan.md`
- 执行结果：
  1. 已恢复 tracked 原路径文档。
  2. 已移除未追踪重复副本。
  3. `git status` 已确认无该文档删除/未追踪组合。
- 验收条件：通过。

## FIX-M001（已完成）
- 背景：认证装饰器依赖全局守卫链，但未显式声明前提。
- 修复目标：显式化依赖前提，降低误用风险，不改变运行行为。
- 涉及文件：
  - `src/authv2/decorators.ts`
- 执行结果：
  1. 在认证装饰器区域新增两行说明注释，明确依赖 `APP_GUARD(ApiKeyAuthGuard/JwtAuthGuard/PermissionsGuard)`。
  2. 未修改函数签名与逻辑。
- 验收条件：通过。

## 3. 原子步骤与验收记录

| 步骤 | 粒度 | 验收条件 | 结果 |
|---|---|---|---|
| S1 | `decorators.ts` 注释级修复 | `npm run typecheck:file -- "src/authv2/decorators.ts"` | ✅ 通过 |
| S2 | auth 装饰器单测 | `npx jest --config "test/jest.unit.config.js" --runInBand "test/unit/authv2/decorators.spec.ts"` | ✅ 通过（1 suite, 4 tests） |
| S3 | 回归单测集合 | `receiver/stream/data-mapper` 相关 4 个 suite | ✅ 通过（4 suites, 33 tests） |
| S4 | 覆盖率回归 | 指定 `collectCoverageFrom` 的回归命令 | ✅ 通过 |

## 4. 测试与覆盖率报告

- 回归命令：
  - `npx jest --config "test/jest.unit.config.js" --runInBand "test/unit/authv2/decorators.spec.ts" "test/unit/core/03-fetching/stream-data-fetcher/services/stream-data-fetcher.service.performance.spec.ts" "test/unit/core/01-entry/receiver/services/receiver.service.history-pipeline.spec.ts" "test/unit/core/00-prepare/data-mapper/services/modules/mapping-rule-engine.module.logging.spec.ts"`
- 结果：`4 passed, 33 passed`

- 覆盖率命令：
  - `npx jest --config "test/jest.unit.config.js" --coverage --collectCoverageFrom="src/core/03-fetching/stream-data-fetcher/services/stream-data-fetcher.service.ts" --collectCoverageFrom="src/core/01-entry/receiver/services/receiver.service.ts" --collectCoverageFrom="src/authv2/decorators.ts" --runInBand "test/unit/authv2/decorators.spec.ts" "test/unit/core/03-fetching/stream-data-fetcher/services/stream-data-fetcher.service.performance.spec.ts" "test/unit/core/01-entry/receiver/services/receiver.service.history-pipeline.spec.ts"`
- 结果摘要：
  - `authv2/decorators.ts`: `Statements 100% / Branch 50% / Functions 100% / Lines 100%`
  - `receiver.service.ts`: `Statements 66.58% / Branch 51.3% / Functions 65.38% / Lines 66.98%`
  - `stream-data-fetcher.service.ts`: `Statements 7.58% / Branch 5.86% / Functions 2.25% / Lines 7.53%`

## 5. 循环终止判定

- 连续两轮扫描：`Critical=0` 且 `Important=0`。
- 所有验收测试：通过。
- 结论：**满足终止条件，循环结束。**
