/**
 * 符号映射相关常量（领域层）
 *
 * 🎯 用途：定义符号映射方向和相关类型
 * 📦 使用：在 symbol-transformer、symbol-mapper-cache 等跨模块位置统一引用
 *
 * 说明：该常量原位于 05-caching/shared/constants，已上移至 core/shared/constants
 * 以消除跨模块对缓存子系统的反向依赖。
 */

/**
 * 符号映射方向枚举
 *
 * 🔄 用途：指定符号转换的方向
 * 📝 应用场景：
 * - TO_STANDARD: 将提供商特定格式转为标准格式 (如 "700.HK" → "00700")
 * - FROM_STANDARD: 将标准格式转为提供商格式 (如 "00700" → "700.HK")
 */
export enum MappingDirection {
  /** 从原始符号转换为标准符号 */
  TO_STANDARD = 'to_standard',
  /** 从标准符号转换为原始符号 */
  FROM_STANDARD = 'from_standard',
}

/**
 * 映射方向类型
 */
export type MappingDirectionType = keyof typeof MappingDirection;

